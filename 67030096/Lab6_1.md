# 🧪 Lab 6-1: การทดลอง ESP-IDF ด้วย Docker

## หัวข้อ: การใช้งาน Docker สำหรับ ESP-IDF
**รายวิชา:** 2568.01 APPLICATIONS OF MICROCONTROLLERS  
**สัปดาห์ที่:** 6  
**เวลา:** 3 ชั่วโมง  

---

## 🎯 วัตถุประสงค์
1. ศึกษาการใช้ Docker สำหรับพัฒนาโปรแกรม ESP32  
2. เข้าใจโครงสร้างไฟล์ CMake และขั้นตอนการ build  
3. ทดลองรันโปรแกรม ESP-IDF ภายใน container  

---

## 🔍 คำตอบคำถามทบทวน

### 1. ความแตกต่างระหว่าง Docker และ Native Setup
| การติดตั้งแบบ Native | การใช้ Docker |
|-----------------|----------------|
| ต้องติดตั้ง ESP-IDF และ dependency เอง | ใช้ image ที่พร้อมใช้งาน |
| เสี่ยง version ไม่ตรงกัน | สภาพแวดล้อมเหมือนกันทุกเครื่อง |
| ลบยาก | สร้าง/ลบ container ได้ง่าย |

---

### 2. ขั้นตอนการ Build
1. รัน `idf.py set-target esp32` เพื่อเลือก target  
2. ใช้ `idf.py build` เพื่อให้ CMake และ Ninja ทำงาน  
3. Compile → Link → สร้าง `.elf` และ `.bin` ในโฟลเดอร์ `build/`

---

### 3. หน้าที่ของ CMake Files
- `CMakeLists.txt` (root): ตั้งชื่อโปรเจกต์และเรียกใช้ `project.cmake`  
- `main/CMakeLists.txt`: บอกว่า main component ใช้ไฟล์อะไรบ้าง  

---

### 4. การใช้ `.gitignore`
ใช้เพื่อกันไม่ให้ไฟล์ build หรือชั่วคราวถูก push เช่น  


build/
*.elf
*.bin
*.log


---

### 5. ข้อมูลที่หายหลังปิด Container
- ✅ ไฟล์ในโฟลเดอร์ที่ mount เช่น `/project` จะยังอยู่  
- ❌ ไฟล์ภายใน container ที่ไม่อยู่ใน volume จะหาย  

---

### 6. เปรียบเทียบ Workflow
| ด้าน | Docker | Native |
|------|---------|--------|
| Environment | คงที่ | ต้องติดตั้งเอง |
| ความยืดหยุ่น | สูง | ผูกกับระบบ |
| Performance | ช้ากว่านิดหน่อย | เร็วกว่า |
| เหมาะกับ | งานกลุ่ม | งานเดี่ยว |

---

## 💡 บันทึกผลการทดลอง

| รายการ | ผลลัพธ์ |
|--------|-----------|
| คำสั่ง build | `idf.py build` |
| ไฟล์ที่ได้ | `build/lab6_1_basic_build.elf` |
| การรัน | `idf.py qemu` |
| ผลลัพธ์ | แสดงข้อมูล IDF version, heap size และ counter เพิ่มขึ้นทุก 2 วินาที |
| สรุป | ✅ Build และ run สำเร็จภายใน Docker |

---

## ✅ สรุปผล
- เข้าใจการใช้ Docker กับ ESP-IDF  
- รู้ขั้นตอนการ build และการจัดการไฟล์ใน container  
- สามารถสร้างและรันโปรแกรมพื้นฐานได้สำเร็จ  
