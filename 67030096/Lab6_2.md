# 🧪 Lab 6-2: การจัดการโปรเจกต์แบบหลายไฟล์ (Multi-File Project)

## หัวข้อ: โครงสร้างโปรเจกต์และ CMake
**รายวิชา:** 2568.01 APPLICATIONS OF MICROCONTROLLERS  
**สัปดาห์ที่:** 6  
**เวลา:** 3 ชั่วโมง  

---

## 🎯 วัตถุประสงค์
1. เรียนรู้การแยกโค้ดเป็นหลายโมดูล  
2. ทำความเข้าใจการจัดการ CMakeLists.txt  
3. ฝึกใช้ Header files และ FreeRTOS Tasks  

---

## 🔍 คำตอบคำถามทบทวน

### 1. เหตุผลในการแยก Source Files
- โค้ดอ่านง่ายและจัดการได้ดีขึ้น  
- ลดความซับซ้อนของไฟล์หลัก  
- ใช้ซ้ำได้หลายโปรเจกต์  

---

### 2. การจัดการ CMakeLists.txt
เมื่อเพิ่มไฟล์ใหม่ (เช่น `sensor.c`, `display.c`, `led.c`)  
ต้องเพิ่มชื่อไฟล์ในส่วน `SRCS` ของ `main/CMakeLists.txt` เพื่อให้ CMake compile รวมกัน

---

### 3. Header Files
- ใช้ประกาศ prototype ของฟังก์ชัน  
- ช่วยให้ไฟล์อื่นเรียกใช้งานได้โดยไม่ต้องเห็นโค้ดภายใน  
- ส่งเสริมการเขียนโปรแกรมแบบ modular  

---

### 4. Include Directories
ต้องมี `INCLUDE_DIRS "."` เพื่อให้ compiler หา header ในโฟลเดอร์ปัจจุบันได้  
หากไม่ใส่ จะ compile ไม่ผ่านเพราะหา `.h` ไม่เจอ  

---

### 5. การใช้ `.gitignore`
ช่วยกันไม่ให้ไฟล์ไม่จำเป็น (เช่น `build/`, `.elf`, `.bin`) ถูก push  

---

### 6. FreeRTOS Task Management
โมดูล LED ใช้ task แยกจาก main เพื่อกระพริบ LED ทุก 3 วินาที  
ข้อดีคือ:
- ทำงานขนานกับงานหลัก  
- ไม่บล็อก loop หลัก  
- สาธิต multitasking บน ESP32  

---

### 7. การจัดระเบียบโค้ด
โมดูลที่ใช้ในโปรเจกต์:
| โมดูล | หน้าที่ |
|--------|-----------|
| `sensor.c` | อ่านค่าจากเซนเซอร์ |
| `display.c` | แสดงข้อมูลบนจอ |
| `led.c` | ควบคุมการกระพริบ LED |
| `main.c` | รวมและจัดการทุกโมดูล |

---

## 💡 บันทึกผลการทดลอง

| ขั้นตอน | จำนวนไฟล์ Source | ขนาด Binary (ประมาณ) | การทำงาน |
|----------|-------------------|------------------------|-----------|
| 1 | 2 (`main.c`, `sensor.c`) | ~300 KB | แสดงข้อมูล sensor |
| 2 | 3 (`main.c`, `sensor.c`, `display.c`) | ~330 KB | แสดงผล sensor + display |
| 3 | 4 (`main.c`, `sensor.c`, `display.c`, `led.c`) | ~360 KB | เพิ่ม LED กระพริบทุก 3 วินาที |

---

## ✅ สรุปผล
- เข้าใจการจัดโครงสร้างโปรเจกต์ ESP-IDF  
- สามารถใช้ CMakeLists.txt จัดการหลายโมดูลได้  
- ใช้ header files เพื่อเชื่อมโยงโมดูลได้อย่างถูกต้อง  
- ทดลองใช้งาน FreeRTOS Task สำหรับงานขนานได้สำเร็จ  
